name: kigali_city_directory
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Firebase & State Management
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  provider: ^6.1.2
  
  # Maps and Navigation
  google_maps_flutter: ^2.6.0
  url_launcher: ^6.2.5
dev_dependencies:
  flutter_lints: ^6.0.0
